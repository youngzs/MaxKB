{
  "_comment": "Gate 3 SKELETON preset workflow for finance document generation. Production wiring lands in Gate 5 once the docx_render_node ships (Track B). Node ids are stable; properties are minimal — the management command will fill in workspace-specific fields (knowledge_base_ids, default model id) at install time.",
  "name": "finance.document_generator",
  "version": "0.1.0-skeleton",
  "description": "Project facts → AI fill long_text placeholders → docx render → confirm.",
  "nodes": [
    {
      "id": "base-node",
      "type": "base-node",
      "properties": {
        "stepName": "基本信息",
        "node_data": {
          "desc": "财务文档生成预设流程（Gate 3 骨架）",
          "name": "finance_document_generator",
          "prologue": ""
        }
      }
    },
    {
      "id": "start-node",
      "type": "start-node",
      "properties": {
        "stepName": "开始",
        "fields": [
          {"label": "项目id", "value": "project_id"},
          {"label": "模板id", "value": "template_id"},
          {"label": "待填占位符列表", "value": "placeholder_keys"}
        ]
      }
    },
    {
      "id": "search-knowledge-node",
      "type": "search-dataset-node",
      "properties": {
        "stepName": "检索项目背景",
        "_skeleton_note": "knowledge_base_ids will be filled from FinanceProject.knowledge_base_ids at install time"
      }
    },
    {
      "id": "ai-chat-node",
      "type": "ai-chat-node",
      "properties": {
        "stepName": "AI 填充长文本占位符",
        "_skeleton_note": "model id + prompt template come from workspace defaults; should build per-placeholder prompts using {label, ai_hint, type=='long_text'}"
      }
    },
    {
      "id": "docx-render-node",
      "type": "finance-docx-render-node",
      "properties": {
        "stepName": "渲染 docx",
        "_skeleton_note": "PROVIDED BY TRACK B (Gate 3) — wraps finance.service.document_generator.render_template"
      }
    },
    {
      "id": "end-node",
      "type": "end-node",
      "properties": {
        "stepName": "结束"
      }
    }
  ],
  "edges": [
    {"sourceNodeId": "start-node", "targetNodeId": "search-knowledge-node"},
    {"sourceNodeId": "search-knowledge-node", "targetNodeId": "ai-chat-node"},
    {"sourceNodeId": "ai-chat-node", "targetNodeId": "docx-render-node"},
    {"sourceNodeId": "docx-render-node", "targetNodeId": "end-node"}
  ]
}
